version: '3.7'

services:
  web:
    hostname: web
    image: web
    environment:
      - FACET_WALLET_PK=${FACET_WALLET_PK}
    build:
      context: .
      args:
        - NPM_TOKEN=${NPM_TOKEN}
    ports:
      - "80:80"
    networks:
       pub-shared-net:

networks:
  pub-shared-net:
    name: pub-shared-net
    external: true